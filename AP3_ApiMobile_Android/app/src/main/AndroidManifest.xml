<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApiMobile"
        android:usesCleartextTraffic="true">

        <activity android:name=".DetailCommandeActivity" />
        <activity android:name=".MesCommandesActivity" />
        <activity android:name=".PanierActivity" />
        <activity android:name=".DetailProduitActivity" />
        <activity android:name=".MainActivity" />

        <!-- Activités admin -->
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".AdminProduitsActivity" />
        <activity android:name=".AdminProduitFormActivity" />
        <activity android:name=".AdminCommandesActivity" />
        <activity android:name=".AdminDetailCommandeActivity" />
        <activity android:name=".AdminUtilisateursActivity" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
